module State

record State s a where
  constructor MkState
  | runState : s → (a × s)

dec get : ∀ s. State s s
def get := MkState (λ s. s × s)

dec put : ∀ s. s → State Unit s
def put := λ s. MkState (λ _. (), s)
