module T

dec tup : Int, Bool, String
def tup := (10, true, "hi")

(* ALIAS TESTS - WORKING *)
(*
alias Boolean := Bool

dec chooseType : Boolean → Type
def chooseType b :=
  match true to
  | true ⇒ String
  | false ⇒ Int

dec v : chooseType true
def v := "hello"
*)

(* UNION TESTS - kinda working *)
(*
data Pair : Type → Type → Type =
  ( × ) : (a, b : Type) → Pair a b

dec fst : (a, b : Type) → Pair a b → a
def fst := λ _ _ (x × _) → x

dec snd : (a, b : Type) → Pair a b -> a
def snd := λ _ _ (_ × y) → y

dec swap : (a, b : Type) → Pair a b → Pair b a
def swap := λ _ _ (x × y) → y × x

dec map : (a, b, c : Type) → (b → c) → Pair a b → Pair a c
def map := λ a b c f (x × y) → x × f y
*)

(* RECORD TESTS *)
(*
record Point : Type =
  constructor MkPoint
  | x : Int
  | y : Int

dec point : Int → Int → Point
def point x y := MkPoint { x; y } (* record punning *)

dec incX : Int → Point → Point
def incX n p := { p where x := p.x + n }

dec incY : Int → Point → Point
def incY n p := { p where y =@ (+ n) } (* field function application *)
*)
