module T

(* ALIAS TESTS - WORKING *)
alias Boolean := Bool

dec bing : Bool → Bool
def bing := λ b. b ~ Boolean

(*
dec chooseType : Bool → U
def chooseType :=
  λ b.
    match b to
    | true ⇒ String
    | false ⇒ Int

dec v : chooseType true
def v := "hello"
*)

(* UNION TESTS - kinda working *)
(*
data Pair : U → U → U =
  ( × ) : (a, b : U) → a → b → Pair a b

def test :=
  let v := ( × ) Int Int 10 10 in
  match v to
  | _ × _ ⇒ True
  | _ ⇒ False

dec fst : (a, b : U) → Pair a b → a
def fst := λ _ _ (x × _). x

dec snd : (a, b : U) → Pair a b -> a
def snd := λ _ _ (_ × y). y

dec swap : (a, b : U) → Pair a b → Pair b a
def swap := λ _ _ (x × y). y × x
*)

(* RECORD TESTS *)
(*
record Bing : U =
  constructor MkBing
  | v : Bool

dec ( + ) : Int → Int → Int

record Point : U =
  constructor MkPoint
  | x : Int
  | y : Int
  (*| b : Bing*)

dec point : Int → Int → Int
def point := 
  λ x y.
  (*let b := MkBing { v := true } in*)
  let z := MkPoint { x; y } in (* record punning *)
  z.x

dec incX : Int → Point → Point
def incX := λ n p. { p where x := p.x + n }

dec incY : Int → Point → Point
def incY := λ n p. { p where y =@ (+ n) } (* field function application *)
*)
