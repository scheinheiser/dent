module ImplicitTest

data ( ≡ ) : (a : U) → a → U =
  Refl : ∀ a. a ≡ a

dec one_plus_one : 1 + 1 ≡ 2
def one_plus_one := Refl

dec sym : ∀ a b. a ≡ b → b ≡ a
def sym Refl := Refl

dec trans : ∀ a b c. a ≡ b → b ≡ c → a ≡ c
def trans Refl x := x
