module TypeExample

union Pair : (a: Type) -> (b: Type) -> Pair a b = 
  Cross : a -> b -> Pair a b

dec fst : Pair a b -> a
def fst (Cross x _) := x

dec snd : Pair a b -> b
def snd (Cross _ y) := y
